- name: add csql command
  lineinfile:
    dest: "/home/{{ cockroach_user}}/.bash_profile"
    line: "alias csql='cockroach sql --certs-dir={{ cockroach_certs_dir}} --host={{ansible_fqdn}}'"
    state: present
    backup: yes